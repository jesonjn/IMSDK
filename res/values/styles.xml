<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="LoginFormContainer">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">16dp</item>
    </style>

    <style name="button_style">
        <item name="android:layout_marginLeft">2dp</item>
        <item name="android:layout_marginTop">2dp</item>
        <item name="android:layout_marginRight">2dp</item>
        <item name="android:layout_marginBottom">2dp</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="backbutton_style">
        <item name="android:textColor">#0022ff</item>
        <item name="android:text">Back</item>
        <item name="android:button">@null</item>
        <item name="android:onClick">onBackClick</item>
        <item name="android:visibility">invisible</item>
    </style>

    <style name="aboutbutton_style">
        <item name="android:onClick">onAboutClick</item>
        <item name="android:background">@null</item>
    </style>

</resources>
